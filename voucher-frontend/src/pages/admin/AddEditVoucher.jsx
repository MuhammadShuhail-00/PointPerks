import React from 'react';

// TODO: Build the AddEditVoucher UI here
// All API calls: src/services/api.js
// Helpers: src/utils/helpers.js
// Auth state: useSelector((state) => state.auth)
// Voucher state: useSelector((state) => state.vouchers)

const AddEditVoucher = () => {
  return (
    <div>
      <h2>AddEditVoucher</h2>
      <p>Build this page UI here.</p>
    </div>
  );
};

export default AddEditVoucher;
